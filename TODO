Maybe add the blinds to improve the immersion (hehe).

Make scoreboard probably call it highest cash-ins.

Make an instructional that explains how to do the tie breaks in casino poker.

To determine the score add 14 * position to the value instead of those large powers of 10 to help ppl with 32-bit computers? The previous scheme makes debugging easier and the performance hit might be negligible. Hopefully python ints are 64-bit
